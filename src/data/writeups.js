// Optional blog/writeups list (Section 6.7 / V3-R7). Writeups.jsx reads
// this, but per the decisions log (Section 8) that component stays
// unmounted — no nav link, not rendered on Home — until the owner
// publishes their first real writeup. Flip it on then.
export const writeups = [
  // { title: '[Title]', description: '[one line on what it covers and who it is for]', link: '[link]' },
]
